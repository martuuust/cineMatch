# Optional: copy to .env in the project root for docker compose
# cp .env.docker.example .env

# TMDB API key for real movie data (optional — falls back to mock movies)
TMDB_API_KEY=your_tmdb_api_key_here
TMDB_BASE_URL=https://api.themoviedb.org/3
